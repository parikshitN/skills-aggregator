type Skill {
    name : String!
    domain: String!
}

input SkillInput {
    name : String!
    domain: String!
}

type Mutation {
    createSkill(input: SkillInput!) : Skill
}

type Query {
    getAllSkills : [Skill!]!
}
